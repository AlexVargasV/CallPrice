HELLO AND WELCOME TO MY PROJECT I'M ALEXANDER VARGAS NICE TO MEET YOU.
First this project has 2 ways that you can test.
the first is execute a ala_problem.php this give you a simple message with the answer, but its you need to modify this file you can acces it.
--------------------------------------------------------------------------------------
The second way is a OperationTest this is a UnitTest that you can modify the parameters and follow this steps.

    1. execute the command "Composer install" this is for dowload all dependencies 

    2. execute the command "composer dump-autoload -o" is for refresh the file.

    3. Finally you need to execute the command "vendor\bin\phpunit tests" this is to run the test.

**into the cv folder you will obtain my CV**

Thanks for you're time and expect go to the next phase.
